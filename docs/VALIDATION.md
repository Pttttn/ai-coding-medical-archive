# Проверки v1.2

Техническая AI-запись от 19 сентября 2026. Все документы и идентификаторы в испытаниях вымышлены. Требования: [спецификация v1.2](../PROJECT_SPECIFICATION_Local_Medical_Archive_v1.2.md); происхождение решений: [конспект и карта изменений](CONSULTATION_CHANGES.md).

## Результаты изменённого слоя

| Проверка | Наблюдаемый результат | Доказательство |
|---|---|---|
| AI pytest в Linux | 143 passed, 0 skipped; Ruff PASS | [Журнал](evaluation/v12-linux-tests.txt) |
| Реальный публичный RAG, 900/120 | 17/21 совместных проверок, первые 10: 10/10; exact facts: 12/12 | [Все результаты](evaluation/v12-public-rag.json) |
| Privacy в этом RAG-прогоне | 20/21 проверенных payload; остальные закрыты ошибкой без raw fallback | [Все результаты](evaluation/v12-public-rag.json) |
| Настоящий HTTP MCP | 4 инструмента, повторная индексация без дублей, отклонение traversal | [HTTP smoke](evaluation/v12-mcp-http-smoke.json) |
| Реальные отрицательные privacy-сценарии MCP | 8/8: имя без метки, клинический фрагмент, прямые запросы ФИО/карты, ошибки пути/типа/аргумента/tool | [Privacy HTTP](evaluation/v12-mcp-privacy.json) |
| Ручная консультация с реальным Ollama | 3/3: идентификаторы удалены, заданные медицинские фразы сохранены | [Manual privacy](evaluation/v12-manual-privacy.json) |
| Полный API сценарий | 20/20, 92.84 с, первый запуск без resume | [Application smoke](evaluation/v12-application-smoke.json) |
| Reference host, qwen3.5:4b | Сам выбрал ask_question, получил проверенные источники и ответ 17 days | [Host с digest модели](evaluation/v12-host-agent.json) |
| Container runtime | 5/5: health, локальные модели, закрытый egress AI/backend/gateway | [Runtime](evaluation/v12-runtime-container.json) |

Unit/HTTP regression suite использует явно обозначенный тестовый адаптер. Реальные оценки используют qwen2.5:3b и nomic-embed-text; имена/digests и параметры сохранены в JSON. RAG проверялся на host Ollama, HTTP и application smoke — с контейнерным Ollama. Некоторые проверки выполнялись параллельно; времена не являются изолированным сравнительным benchmark.

Backend и frontend не изменялись: прежние 48 и 22 теста, browser 15 и PDF 4/4 подробно сохранены в [исторической приёмке v1.1](VALIDATION_v1.1.md). Они не выдаются за повторённые новые локальные прогоны. Полный новый API smoke отдельно подтвердил совместимость общего privacy слоя и exact review/export. Финальная узкая поправка проверки адреса подтверждена тремя реальными manual cases и полным AI suite; общий API/host сценарий выполнялся перед этой поправкой.

## Изменения и обнаруженные ошибки

Корпус теперь содержит 42 файла MD/TXT/PDF: 663711 файловых байт, из них 661410 байт обычного индексируемого текста, достаточных для порога 512000 без PDF overhead. Manifest проверен по рабочим файлам и по Git blobs после нормализации LF. Seed по-прежнему 36 записей, неизменяемые источники и 5 исправлений. Реальная миграция MCP-индекса: 45/777 → 42/773 (файлы/чанки), удалены 5 запрещённых форматов. Последующие index_folder ничего не дублируют.

Публичные поля ограничены очищенным текстом, reference S1/S2 и случайным responseRef. Локальная таблица соответствий не доступна через MCP. Весь сериализованный ответ проверяется, включая ошибки. Проверены недоступность модели, неверный JSON, неизвестные поля, несуществующие spans, числовые коллизии, единицы, даты, отрицания и неизвестность приёма. Цитаты собираются структурно, поэтому медицинские [17]/[1] не превращаются в номера ссылок.

Реальная модель выявила проблемы сверх тестовых адаптеров: подстановка частей имени ломала email до его удаления; имя после fictional participant не очищалось; адрес 15 Pine Street ошибочно считался числом с единицей. Исправлены порядок обработки, общий шаблон narrative name-cue и проверка адресов. Неудачные попытки сохранены: [email](evaluation/v12-public-rag-before-email-order.json), [имя без метки в MCP](evaluation/v12-mcp-http-before-name-cues.json), [ложный отказ адреса](evaluation/v12-manual-privacy-before-address-guard.json). Старые privacyPassed flags отражают набор проверок на момент запуска, а не итоговое отсутствие утечек.

## Известные ограничения

Не прошли совместную текущую RAG-проверку: **overview-01, multi-01, negation-01, conflict-01**. Обзор выбирает слишком узкий источник; вопрос о двух визитах и отдельный вопрос о неизвестном приёме могут получить ложный отказ; противоречие Spruce не подтверждается полным ожидаемым набором источников и блокируется строгой privacy-проверкой. Эти случаи сохранены, а не исключены из знаменателя.

Метрика проверяет заданный текст, нужные источники, ожидаемый отказ и известные вымышленные идентификаторы. Она не измеряет медицинскую правильность и не доказывает полную анонимность. Ручной preview/hash остаётся обязательным для личного экспорта. MCP автоматически передаёт очищенную синтетику host-агенту; доступ к личному корпусу и внешние AI SDK не добавлены.

Новая дата защиты и принятие собственной темы не подтверждены; по авторским записям тема отправлена на утверждение 14 сентября. Личная часть REPORT оформлена по предоставленной автором хронологии и реальным запросам, отдельно от AI-приложений. Reference host действительно использует MCP, но не заменяет проверку конкретного IDE пользователя.

## Git и воспроизведение

Первый опубликованный CI [35439096699](https://github.com/ruslan-yusupov-open/ai-coding-medical-archive/actions/runs/35439096699) выявил различие CRLF/LF в manifest. Генератор теперь пишет LF; проверка staged Git blobs прошла. Новый CI подтвердил исправление на опубликованной ревизии; результат приведён ниже и отделён от локальных модельных оценок.

Команды находятся в [README](../README.md), [AI README](../ai-service/README.md) и [журнале оценок](evaluation/README.md). Реальные результаты v1.1 сохранены без перезаписи; текущие JSON имеют префикс v12-. В Git не добавлены attachment, .env, индексы, архивные данные или содержательные runtime logs.

### Подтверждённый опубликованный CI

[GitHub Actions 35442666973](https://github.com/ruslan-yusupov-open/ai-coding-medical-archive/actions/runs/35442666973) на commit **e02afee** завершился успешно: backend **48**, frontend **22**, AI **143** — всего **213 passed**. Lint, application builds, corpus/seed checks, обе Compose-конфигурации и сборка всех Docker images PASS. [Структурированный результат](evaluation/v12-github-ci.json). Это повторная проверка backend/frontend на GitHub, в дополнение к явно отделённым прежним локальным прогонам. Последующие правки этой записи относятся только к документации.

Gitleaks v8.30.1 проверил полную историю до e02afee: 12 коммитов, около 3.53 MB, **0 findings**. Отдельно подтверждено отсутствие отслеживаемых .env, runtime directories, баз и индексов. Вымышленные имена в fixtures и сохранённых неудачных оценках намеренны.


## Повторный аудит и чистый Docker-запуск

19 сентября 2026 выполнен [аудит формальных требований и пожеланий преподавателя](REQUIREMENTS_AUDIT.md). Исходная ревизия 4680ced клонирована заново с GitHub; использованы новые пустые тома PostgreSQL, оригиналов, обоих индексов и Ollama. Без `.env`, host Ollama, предварительных зависимостей или ручной подготовки весов `docker compose up -d --wait` собрал сервисы и загрузил модели. Использованы отдельное имя проекта и порты, поскольку рабочие сервисы оставались запущенными; Docker image/build cache не очищался.

Новые реальные результаты: [application 20/20 за 79.89 с](evaluation/cold-start-application.json), [PDF 4/4](evaluation/cold-start-pdf.json), [browser 15 без JS errors/external requests](evaluation/cold-start-browser.json), [HTTP MCP EMPTY → 42/773 → ask/find/reindex](evaluation/cold-start-mcp.json), [runtime 5/5](evaluation/cold-start-runtime.json). Модели контейнерные, CPU. Эти результаты отдельно подтверждают чистый старт, а не заменяют расширенную оценку RAG 17/21.

Исправлен AI healthcheck: HTTP 200 недостаточно, теперь требуется ready=true; проверено с реальной остановкой/восстановлением Ollama. Gateway ожидает healthy upstream и проверяет публичные UI/API. После down/up с обновлённым Compose полностью совпали хеши 11 таблиц БД, 36 seed READY и MCP 42/773; [снимки и подробности](REQUIREMENTS_AUDIT.md). README теперь содержит clone/cd/up, критерии готовности, диагностику и сохранение данных.

Опубликованное исправление **4bb2205** повторно клонировано с GitHub и запущено с ещё пятью пустыми томами. `up --wait` завершился примерно за 131 с с использованием image/build cache и новым автоматическим скачиванием моделей; [шесть healthy, 36 READY, MCP EMPTY](evaluation/cold-start-final-fresh.json). [CI 35445509270](https://github.com/ruslan-yusupov-open/ai-coding-medical-archive/actions/runs/35445509270) для этого коммита также успешен, включая все четыре jobs и Docker images.

На финальном чистом экземпляре дополнительно успешны [настоящий HTTP MCP](evaluation/cold-final-mcp.json) и [runtime 5/5](evaluation/cold-final-runtime.json): пустой индекс, 42/773 после индексации, проверенный ответ 17 days, отсутствие дублей и ограничения сети.
